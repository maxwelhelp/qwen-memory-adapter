# Обучаемая MLP-память для Qwen (qwen-memory-adapter)

Персистентная память для замороженной Qwen2.5-0.5B-Instruct: **память живёт в весах
маленькой MLP-сети**, а не в KV-кэше. Qwen читает контекст в «сессии 1» — память
записывает его в свои веса градиентными шагами. В «сессии 2» (новый промпт, кэш
Qwen выброшен) память подмешивает знание обратно — Qwen отвечает так, как будто
контекст ещё перед ней.

```
Сессия 1:  контекст (секрет в мусоре) → память пишет (веса MLP = память)
Сессия 2:  только вопрос → Qwen без контекста + подмешивание из памяти → правильный ответ
```

## Результаты (кратко, подробно — docs/RESULTS.md)

Межсессионный тест (40 примеров: 4 типа «Пароль/Код/Пин-код/Секрет: X» в мусоре):

| Версия памяти | Правильных ответов |
|---|---|
| Без памяти (шанс) | 5/40 (12%) |
| **Линейные ассоциативные слоты, η из энтропии Qwen (exp2n, ЛУЧШАЯ схема)** | **40/40 (100%)** |
| Линейные ассоциативные слоты, η=mask_secret (exp2h) | 34-36/40 (85-90%) |
| Линейные слоты (старая схема v5) | 34/40 (85%) |
| MLP-слоты + η-голова («что писать») | 24/40 (60%) |
| MLP-слоты | 23/40 (57%) |
| С контекстом (эталон, dense) | 40/40 |

**Рабочий рецепт (как это собрано и почему): `docs/RECIPE_WORKING_MEMORY.md`.**
Коротко: выравнивание (M(h)≈h, cos 0.98) → **η-гейт из удивления ОСНОВНОЙ СЕТИ**
(энтропия предсказания Qwen на токенах контекста — бесплатно при forward, без учителя)
→ ассоциативная delta-rule запись пар (k_t→v_{t+1}) в **линейные** слоты (нелинейность
ломает retrieval — главный урок серии) → retrieval-чтение (softmax(q·W_route) → Σ w_i·W_i·q)
→ сдвиг логпробов кандидатов.

## Как это работает

1. **Память = слоты**: 32 слота × MLP (896→H→896, GELU, без bias, xavier) —
   веса слотов и есть память.
2. **Запись** (необучаемая механика, Titans-правило): для каждого токена контекста
   градиентный шаг самовосстановления `θ ← θ(1−γ) − η·∇||M(h)−h||²`, нормированный,
   чанками (CHUNK=16), ITERS итераций. **η-голова** (`σ(W_eta(h))`) решает «как сильно
   писать токен» (учитель: секрет=1, мусор=0.1) — novelty гейтит запись.
3. **Чтение** (обучаемое): веса слотов выбираются маршрутизацией `softmax(q·W_route)`;
   mix = g·W_out(Σ w_i·(M_i(q; запись) − M_i(q; θ₀))) — 6 чисел = сдвиг логпробов
   кандидатов; финальные логиты = логиты без контекста + mix.
4. **Обучение** (Qwen заморожена):
   - Фаза 1 — **alignment**: слоты учатся воспроизводить скрытые состояния Qwen
     (M(h)≈h) — память «говорит на языке Qwen»;
   - Фаза 1.5 — **η-голова** (что писать);
   - Фаза 2 — **дистилляция**: слоты заморожены, обучаются W_route, W_out, g
     (loss = CE по кандидатам + MSE сдвига логпробов).

## Структура проекта

### Скрипты (в корне)

| Файл | Что делает |
|---|---|
| `gen_yattn.py` | Генерирует датасет дистилляции: 400 примеров (4 типа задач), Δ = выход с контекстом − без, mask_secret для η-головы → `dataset_yattn.pt` |
| `train_memory_distill.py` | Обучение памяти: alignment → η-голова → дистилляция → `memory_distill.pt` |
| `eval_cross_session.py` | **Межсессионный тест** (главная метрика): сессия 1 пишет, сессия 2 отвечает без контекста |
| `smoke_memory_slots.py` | Синтетический тест механики записи (слоты, η/γ, чанки, изоляция, персистентность) |
| `gen_knowledge_dataset.py` | Шаг 1: датасет know/not-know с эмпирическими лейблами (срез знаний) → `dataset_knowledge.pt` |
| `train_knowledge_clf.py` | Шаг 1: классификатор know/not-know (92%) — онлайн-сигнал новизны → `clf_knowledge.pt` |
| `eval_memory.py` | Абляция: baseline / пустая память / с записью |
| `eval_delta_check.py` | Контроль: содержит ли Δ (эффект контекста) правильную информацию |
| `exp2h_linear_assoc_eta.py` | **НОВАЯ РАБОЧАЯ СХЕМА**: линейные ассоциативные слоты + delta-rule запись + η-гейт + проекции → 36/40 (90%) |
| `eval_exp2h.py` | Межсессионный тест для exp2h (главная метрика новой схемы) |
| `exp2f_surprise_test.py` | Удивление ‖M(h)−h‖² различает секрет/мусор (AUC 0.89) — сигнал «что писать» без учителя |
| `versions/` | **Архив ВСЕХ версий кода памяти** (v1→v5): delta-rule, первая MLP-попытка, smoke Titans, smoke слотов, рабочая версия — с описанием результата каждой (versions/README.md) |

### Документация (docs/)

| Файл | О чём |
|---|---|
| `ARCHITECTURE.md` | **Архитектура памяти (сводный файл)**: компоненты, формулы, фазы обучения, инференс, ключевые решения, метрики |
| `01-memory-architecture-kv-fastweights.md` | **Свод идей** (внешний, для сравнения): обзор 14 направлений — тернарная адресуемая память (BitNet-стиль), LoRA-память, alignment-дистилляция, SAE-ключи, relative representations, Coconut. Подтверждает наши решения (alignment, слоты как MoE, детектор→запись) и предлагает новое: тернарная дискретная запись, якорные представления без обучения |
| `04-online-mlp-memory-qwen.md` | **Внешний дизайн-документ**: онлайн-память (MLP) для Qwen — дистилляция пространства + sparse-обновление активных нейронов + gated-инъекция delta-вектора. Опирается на ROME/MEMIT, Procrustes, atlas/patch/rel_err. Схожа с нашей схемой (gate + delta в hidden), но предлагает sparse-обновление нейронов вместо градиентных шагов по всем весам |
| `memory_layer_v2.md` | Архитектура памяти (исходный план): запись/чтение/подмешивание, η/γ, персистентность между сессиями (раздел 4c) |
| `titans_qwen.md` | План + фиксы логических дыр (сигналы новизны из одного forward, классификатор know/not-know, разбор 2026-08-03) |
| `titans_qwen2.md` | Развитие: двухуровневая память L_fast/L_slow, persistent tokens (из Anamnesis/ATLAS) |
| `REPORT_MEMORY_MLP.md` | **Отчёт план vs реализация**: что сделано по плану, что другим способом, почему MLP не работал, как MLP устроен в Titans (по коду), архитектура и решения проблем, план возврата (3.1/3.2/3.3) |
| `RESULTS.md` | **Результаты всех экспериментов**: скоринг/эвикция (80% vs 20%), классификатор know/not-know (92%), дистилляция памяти, межсессионные тесты, MLP-версия |
| `tiered_memory_mlp.md` | Развитие: трёхуровневая память (кэш + векторная база + retrieval) |
| `annotator_teacher.md` | Развитие: аннотатор-учитель (важность токенов с будущим вопросом) |
| `kv_mechanisms.md` | Референс: механики удаления/добавления KV, удивление, забывание (Titans, H2O, EpiKV и др.) |

## Версии кода памяти (хронология)

Все итерации «как делалась память» — от ранних попыток до рабочей версии.
Полное описание — `versions/README.md`, разбор провалов — `docs/REPORT_MEMORY_MLP.md`.

| Версия | Путь | Что делает | Результат |
|---|---|---|---|
| v1 | `versions/v1_train_memory.py` | delta-rule по головам внимания, дистилляция на y_attn (выход внимания), обучаемые η и проекция выхода | Линейная delta-память не аппроксимирует внимание (cosine ~0) — заброшена |
| v2 | `versions/v2_train_mlp_memory.py` | **Первая MLP-память** (Titans-стиль, 896→3584→896) + дистилляция y_attn, запись батч-градиентами с create_graph | MLP **схлопнулся к нулю**: 2 шага записи на весь контекст, нет η/γ, нет учителя на уровне дистилляции |
| v3 | `versions/v3_smoke_memory.py` | Smoke готового NeuralMemory из titans-pytorch (без изменений) | Референс: рабочая механика Titans из коробки |
| v4 | `versions/v4_smoke_memory_slots.py` | Синтетический тест СЛОТОВ: запись пар (k→v) чанками, η/γ, изоляция слотов, γ-стирание, перезапись фактов, персистентность save/load, L_fast/L_slow | Механика жива: запись −44% ошибки (линейный слот), γ-стирание, изоляция, перезапись работают |
| v5 | `versions/v5_train_memory_distill.py` (копия рабочей из корня) | **Рабочая версия**: alignment (M(h)≈h) → η-голова («что писать») → дистилляция сдвига логпробов кандидатов; слоты линейные или MLP — конфигом | Межсессионный тест: линейные **34/40 (85%)**, MLP+η **24/40 (60%)**, шанс 5/40 (12%) |

## Запуск

Зависимости: Python 3.12, torch 2.6.0+cu124 (Tesla P40, sm_6.1), transformers 4.57.6.

```bash
# 1. датасет дистилляции (400 примеров, ~10 мин на P40)
python gen_yattn.py

# 2. обучение памяти (alignment + η + дистилляция, ~1-2 ч)
python train_memory_distill.py

# 3. межсессионный тест — главная метрика
python eval_cross_session.py
```

Опционально (шаг 1 — сигнал новизны для η/θ-гейта):
```bash
python gen_knowledge_dataset.py   # датасет know/not-know (эмпирические лейблы)
python train_knowledge_clf.py     # классификатор (92%)
```

Данные/веса (`*.pt`) в git не хранятся — генерируются скриптами (см. .gitignore).

## Железо / контекст

- Модель: Qwen/Qwen2.5-0.5B-Instruct (hidden 896, 26 слоёв, eager attention — P40
  не поддерживает flash-attn2).
- Всё обучение — только память; Qwen заморожена (никакого end-to-end).
- Память между сессиями: `memory_distill.pt` сохраняется на диск и загружается
  в новой сессии — кэш Qwen выброшен, память помнит.

## Известные ограничения

- MLP-версия уступает линейной в текущей схеме (разность откликов у нелинейных
  слотов слабее); план её усиления — в `docs/REPORT_MEMORY_MLP.md` (3.3: полный
  bilevel TTT-стиля).
- 0.5B не умеет применять «правила» даже с контекстом — правило-задача отложена
  до 1.5B.
