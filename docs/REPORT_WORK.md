# Рабочий журнал проекта (REPORT_WORK)

Правило: дополнять ПОСЛЕ КАЖДОГО завершённого шага/эксперимента (что сделано, цифры,
вывод, расхождения с планом). Коммит+пуш после каждой записи.

---

## 2026-08-04 — Старт: изучение проекта, план V3

**Что сделано:**
- Изучены: `README.md`, `versions/v5_train_memory_distill.py`, `docs/00_READ_ORDER.md`,
  `docs/PLAN_MEMORY_V2.md`, `docs/MEMORY_MASTER.md` (882 строки, весь свод),
  `docs/ARCHITECTURE.md`, `eval_cross_session.py`, `gen_yattn.py` (поля датасета).
- Закоммичены и запушены неотслеживаемые доки (a6a996f): 00_READ_ORDER, MEMORY_MASTER, PLAN_MEMORY_V2.
- Составлен `docs/PLAN_MEMORY_V3.md` — 8 фаз с гейтами, дочитываниями, экспериментами.

**Ключевые факты (из документов):**
- Рабочая схема: логпроб-сдвиг 6 кандидатов; межсессионный тест — линейные слоты 34/40 (85%),
  MLP+η 24/40 (60%), шанс 5/40 (12%).
- v5 = полномасштабный MLP-прогон (H_SLOT=896, N_TRAIN=320, EPOCHS=40) — НЕ доведён до конца.
- Находка §1: докстринг v5 (input-injection) ≠ код (logit-shift); `target_in` в датасете есть,
  в лоссе не участвует. Решение — за пользователем (гейт Ф1).
- Классификатор know/not-know (92%, AUC 0.974) обучен, НЕ подключён к пайплайну.
- `.pt` файлов на диске нет — датасеты/веса генерируются заново (в git не хранятся).
- Окружение: Tesla P40 24GB; git remote = github.com/maxwelhelp/qwen-memory-adapter.

**Открытые решения (гейты):**
1. Ф1: logit-shift vs input-injection (по данным эксперимента 1a).
2. Ф4: замена η-учителя на классификатор или композиция.
3. Ф6: глубина continuous-learning.
4. Ф7: MoE-маршрутизация записи (риск bilevel).

**Статус:** план V3 составлен, ждёт одобрения; Фаза 0 не начата.

## 2026-08-04 — Одобрение плана + дополнение MoE (вариант пользователя)

**Решение пользователя:** план V3 одобрен. Дополнение к Ф7: «Слоты — это уже MoE?» —
разбор пользователя (структурно MoE-паттерн, но гибрид с асимметрией: запись —
случайные route_keys через argmax (не обучается), чтение — обучаемый softmax (W_route),
dense, не top-k). Гипотеза: случайная маршрутизация записи — вероятная причина слабости
типа «Код» (4/10, RESULTS.md).

**План обновлён (Ф7 переработан, закоммичено):**
- 7a — обучаемая маршрутизация записи (без возврата bilevel: дистилляцией, как W_route);
- 7b — top-k чтение (TOPK=4) + load-balancing loss (против схлопывания маршрутизации);
- 7c — полный пакет (7a+7b) vs dense-гибрид 34/40 на том же датасете.
- Критерий: per-type метрика, «Код» отдельной строкой. Старт — по решению пользователя.

**Статус:** план одобрен, Фаза 0 начинается.

## 2026-08-04 — Ф1: указание пользователя + запуск батареи проверок обучаемости

**Указание пользователя (дословно по смыслу):** упрощённые версии за победу не выдавать;
нужна ПОЛНОФУНКЦИОНАЛЬНАЯ ОБУЧАЕМАЯ ПАМЯТЬ; нет смысла идти по плану, пока не проверено
на разных вариантах архитектур — учится ли память и даёт ли пользу. Alignment сделан
(пространства общие), вопрос — обучаемость записи. Сохранено в памяти агента
(feedback: no-simplified-versions).

**План обновлён:** Ф1 = батарея A (контроль, логпроб-сдвиг) / B (векторная инъекция во вход
последнего слоя, открытый словарь — `exp1b_input_injection.py`) / C (bilevel-минимум,
TTT-арифметика без detach — `exp1c_bilevel_smoke.py`). Коммит 3115d5b.

**exp1c (bilevel-минимум) — РЕЗУЛЬТАТ: запись ОБУЧАЕМА.**
- Проблема №1 теста (исправлена): слот не был выровнен (отклик нормы ~300 на входе 15 —
  в v5 это фаза 1 alignment) + шаги не нормированы (взрыв). Добавлен alignment-претест и
  нормированные шаги (ĝ=g/||g||) как в v5.
- Сравнение режимов (1 слот, 16 пар, S=4, 400 шагов, нормированные шаги):
  - **detach (как v5):** |grad_η| = 0 — градиент НЕ доходит; read_loss(текущая пара)
    болтается 2k-7k; η/γ застыли.
  - **bilevel (арифметика без detach, create_graph=True):** |grad_η| ~ 1e2-1e3 — градиент
    ДОХОДИТ до η/γ/θ₀ через все шаги записи; read_loss(текущая) 4634 → 24 (в 190×);
    η: 0.313→0.378, γ: 0.119→0.180 — обучаются.
- read_loss(свежая пара) не падает — ожидаемо: 1 слот на 16 пар, последняя запись
  перезаписывает (γ растёт); в реальной системе это решается 32 слотами + routing.
- **Вывод: detach-клоны (обход bilevel в v5) — причина «память не училась»; Titans-путь
  (арифметика, граф через шаги) жизнеспособен. Полный bilevel TTT — главный кандидат
  на «память учится всегда».**
- Ограничение теста: alignment был недоучен (200 шагов vs ~76800 в v5) — абсолютные
  цифры не показатель, важен качественный сигнал detach vs bilevel.

**exp1b (векторная инъекция) — баг найден и исправлен, полный прогон запущен.**
- Первый прогон упал: `autograd.grad: element 0 of tensors does not require grad`
  в write_work на ПЕРВОМ val-примере (не в train — 320 train-примеров прошли).
- Причина (отлажено пошагово): я обернул val-цикл в `with torch.no_grad()`, а внутри
  `write_work` создаёт клоны с `requires_grad_(True)` — в no_grad-контексте граф не
  строится → iloss без grad_fn → падение. В v5 val-цикл без no_grad — поэтому smoke
  работал. Изолированное воспроизведение фаз падение не показывало, пока не дошло
  до val с no_grad (ложная локализация — print i=319 дважды: последний train и первый val).
- Фикс: write_work вне no_grad (как v5), чтение/метрики — под no_grad. Полный прогон
  12 эпох запущен (~30-40 мин).

## 2026-08-04 — Ф1: exp1b РЕЗУЛЬТАТ (векторная инъекция) + ORACLE-тест

**exp1b (12 эпох, полный прогон) — векторная инъекция в НАИВНОЙ форме НЕ работает:**
```
эпоха 11: val_mse 2.18 | acc6 0.03 | acc_open 0.00 | ‖mix−Δ_in‖ 189 | g=0.67
```
- acc6 0.03 — ниже шанса (0.167); acc_open 0 — открытый словарь не открывается;
- mix РАЗДУВАЕТСЯ: ‖mix‖ 128→189 (нужно ~36), g растёт 0.56→0.69 — модель «кричит»
  вместо того, чтобы попасть в норму. Градиенты MSE на большом масштабе слабые.

**ORACLE-тест (идеальная инъекция = сам target_in во вход последнего слоя, N=40):**
```
acc6_oracle   = 1.00   ← последний слой ПРОПУСКАЕТ правильное возмущение
cos(Δ_oracle, Δ_real) = 0.47
‖Δ_oracle‖/‖Δ_real‖   = 0.16   (‖Δ_out_real‖ ~ 276, усиление слоя ~7.7× от ‖Δ_in‖~36)
acc_open_oracle = 0.07 (первый токен секрета не argmax по всему словарю)
```
- **Направление векторной инъекции ЖИВОЕ**: последний слой усиливает 36-возмущение входа
  в 276-сдвиг выхода и даёт правильный ответ (6 кандидатов, 100%). Проблема exp1b —
  не точка инъекции, а ОБУЧЕНИЕ МАСШТАБА mix (160-190 вместо 36).
- acc_open 0.07 у oracle: полный словарь не открывается даже идеальной инъекцией —
  метрику acc_open для этой задачи уточнить (первый токен секрета vs ответ цельными
  строками) — приоритет ниже, чем чинить масштаб.

**Следующий шаг: exp1b2** — лосс с прямой регрессией mix на target_in
`MSE(mix, target_in) + 0.5·CE + 0.1·MSE(h_out', h_outB)` — oracle показал: mix≈target_in
⇒ правильный ответ. Обучаем mix ПОПАДАТЬ В target_in (норма 36), а не подбирать его
через косвенный MSE на выходе (который и раздувал масштаб).

## 2026-08-04 — exp1b2 запущен

- Скрипт `exp1b2_input_injection.py` создан на базе exp1b (коммит f0d7259): лосс
  `loss = MSE(mix, target_in) + 0.5·CE(cand_logits(h_out'), tgt) + 0.1·MSE(h_out', h_outB)`,
  в train-лог добавлена mix-часть лосса. Полный прогон 12 эпох, мониторится.
- Критерии: mix-часть лосса падает (‖mix‖ → ~36), acc6 растёт выше шанса, acc_open > 0.
- План V3 обновлён блоком «Результаты батареи на 2026-08-04» (C работает, B наивный нет,
  oracle оживляет B, B2 идёт).

## 2026-08-04 — exp1b2 ФИНАЛ + cos-диагностика: ДИАГНОЗ

**exp1b2 (12 эпох, полный):**
```
эпоха 11: ‖mix−Δ_in‖ 29.9 | acc6 0.05 | acc_open 0.00 | mix-часть 0.0036 (застыла)
```
Сходимость остановилась на 3-й эпохе (31.1 → 29.9 за 9 эпох), acc ниже шанса.

**cos-диагностика (веса exp1b2, N=40):**
```
cos(read_diff(q, work), target_in) = 0.0141   ← уровень шума (шум 0.02-0.05)
```
**Отклики слотов ПОСЛЕ необучаемой v5-записи НЕ содержат направления секрета.**

**Выводы (важные, меняют картину):**
1. Векторная инъекция (B) при необучаемой записи НЕВОЗМОЖНА: mix'у нечего передать —
   сигнала нет (cos ≈ 0). Оба лосса (MSE на выходе и регрессия на target_in) бессильны.
2. Объяснение v5: 34/40 (линейные) / 24/40 (MLP+η) — это НЕ «слоты хранят знание»,
   а линейный классификатор W_out вытаскивает слабый сигнал из шумной разности.
   MLP проигрывает линейным, потому что их read_diff шумнее (направленность ниже).
3. Запись-самовосстановление (необучаемая) НЕ кодирует информацию — она хранит «похожесть
   на вход», а не «ответ на вопрос». Полная память требует, чтобы запись кодировала
   ТО, ЧТО НУЖНО ДЛЯ ЧТЕНИЯ — это даёт только bilevel (exp1c: градиенты доходят).
4. Oracle (acc6=1.00 при идеальной инъекции) остаётся в силе: последний слой пропускает
   правильный вектор — вопрос только в том, чтобы слоты его вырабатывали.

**Следующий шаг: exp1d — ПОЛНЫЙ bilevel (запись обучается под чтение).**
Внешний лосс (MSE(mix, target_in) + CE) течёт через шаги записи (create_graph=True)
в θ₀/η/γ → слоты учатся кодировать направление секрета. Критерий: cos(read_diff, target_in)
растёт с 0.014 (шум) до >0.1 на val. Урезанная диагностика: 4-8 слотов, ITERS=2,
N_TRAIN=32, батч 1.

## 2026-08-04 — exp1d РЕЗУЛЬТАТ + ИТОГ БАТАРЕИ Ф1

**exp1d (полный bilevel, 4 слота, ITERS=2, 8 эпох, N_TRAIN=32) — НЕ закодировал:**
```
val: cos(read_diff, target_in) = 0.007–0.056 (колеблется на шуме, не растёт)
val: acc6 = 0.12–0.15 (шанс) | ‖mix‖ ~ 37–52 | η≈0.32 (не двигается), γ≈0.12
```
Train-лосс падает (7.7→5.7) — модель оверфитит CE на 32 примерах дешёвым путём,
слоты направление НЕ кодируют.

**Итог батареи Ф1 (A/B/B2/C/C-full):**

| Вариант | Результат | Вывод |
|---|---|---|
| A: логпроб-сдвиг 6 чисел (v5) | 34/40 линейные, 24/40 MLP | работает, но это классификатор по шумной разности, НЕ векторное хранение |
| B: векторная инъекция (MSE на выходе) | acc6 0.03, mix раздувается 128→189 | не сходится: масштаб |
| B2: регрессия mix→target_in | acc6 0.05, ‖mix−Δ_in‖ застрял 29.9 | не сходится: СИГНАЛА НЕТ (cos=0.014) |
| C: bilevel-минимум (синтетика) | градиенты доходят, лосс в 190× | запись МЕХАНИЧЕСКИ обучаема |
| C-full: bilevel на реальных данных | cos не растёт (0.007-0.056), acc6 шанс | внешний сигнал через 2 нормир. шага слаб / маршрутизация не связывает запись-чтение |

**Главные факты:**
1. Запись-самовосстановление (||M(h)−h||) НЕ кодирует знание — ни обучаемая, ни нет:
   слоты хранят «похожесть на вход», а не «ответ на вопрос» (cos=0.014).
2. Bilevel-механика работает (exp1c), но в exp1d внешний сигнал не продавил кодирование
   за 8 эпох: 2 нормированных шага записи + случайная маршрутизация + слабый внешний
   градиент (нормировка ĝ=ĝ/||ĝ|| режет и внешний поток).
3. Oracle (acc6=1.00 при идеальной инъекции) в силе: последний слой пропускает
   правильный вектор — вопрос в выработке этого вектора слотами.

**Следующий шаг: exp1e** — bilevel с 6-числовой головой чтения (как рабочая v5-схема,
но запись ОБУЧАЕМАЯ): target6 (сдвиг логпробов кандидатов) вместо 896-мерного target_in.
Меньше требований к направлению (6 чисел, а не 896-вектор) → слоты должны легче
закодировать то, что нужно чтению. Усиления: ITERS=4, N_TRAIN=64, 15 эпох.

## 2026-08-04 — exp1e РЕЗУЛЬТАТ + ГИПОТЕЗА МАРШРУТИЗАЦИИ

**exp1e (bilevel, 8 слотов, 6-числовая голова, 15 эпох) — НЕ закодировал:**
```
val: acc6 = 0.15 (шанс) во все 15 эпох | ‖mix6−target6‖ 8.3-10.2 (не падает)
η: 0.32→0.27, g: 0.48→0.39 (сдуваются), γ: 0.12→0.14
```
Train-лосс падает (39.8→15.0) — дешёвый путь, не кодирование.

**Ключевое наблюдение:** exp1c (1 слот, синтетика) — bilevel РАБОТАЛ (лосс в 190×);
exp1d/1e (8 слотов, реальные данные) — НЕ работает. Главная разница: в exp1c запись и
чтение в ОДНОМ слоте; в exp1d/1e маршрутизация: запись — случайные route_keys через
argmax (недифференцируемо), чтение — обучаемый W_route. Секрет пишется в слот A, вопрос
читает слот B → сигнал теряется (совпадение ~1/8).

**exp1g (запущен):** то же, что exp1e, но N_SLOTS=1 — запись и чтение гарантированно в
одном слоте. Если acc6 вырастет — проблема в маршрутизации (чинить её); если нет —
проблема в самом потоке внешнего градиента через самовосстановительные шаги (чинить
запись: внешний сигнал в inner loop).

## 2026-08-04 — Фаза 0: окружение, датасет, диагностика §1

**Окружение:** OK — torch 2.6.0+cu124, CUDA доступна (Tesla P40), transformers 4.57.6,
Python 3.12.3, Qwen2.5-0.5B-Instruct в кэше HF (954M), диск 82G свободно.

**Датасет:** `gen_yattn.py` → `dataset_yattn.pt`: 400 примеров (4 типа × 100),
70100 токенов контекста. Время генерации ~10 мин.

**Диагностика §1 (важно для гейта Ф1) — ПОПРАВЛЕНО 2026-08-04:**
- Генератор печатает mean квадрата КОМПОНЕНТА (по 400×896 элементам): 1.46514 —
  это НЕ ‖Δ_in‖². Реальная норма вектора `‖Δ_in‖ ≈ 36` (подтверждено прямым замером:
  mean ‖target_in‖ = 35.65 на первых 40 примерах). Ранняя запись «≈1.21» — ошибка.
- **Заявление докстринга «норма ~5-10» завышено в ~3.5-7 раз** (реально ~36), но это не
  150. Эксперимент 1a (exp1b) показал: дело не в норме target_in, а в обучении масштаба mix.

**Сверка протоколов (Ф0.5):** механика в `eval_cross_session.py` и в val внутри v5
идентична (write_work(ctx_hidden из прогона B) + mix по q из прогона A + base_cand без
контекста). НО: val в v5 — фиксированные 40 примеров из датасета, а eval_cross_session
генерирует 40 СВЕЖИХ примеров (rng=7, 10 на тип). Цифры сопоставимы качественно,
но не тождественны — при сравнении указывать, какой протокол.

**Статус:** smoke-прогон v5 (урезанный: N_TRAIN=48, EPOCHS=3, align 2 эпохи) запущен —
проверка воспроизводимости пайплайна от свежего датасета.

## 2026-08-04 — Фаза 0: smoke-прогон v5 (результат)

**Smoke:** `N_TRAIN=48, EPOCHS=3` применились (sed не зацепил вложенные `ALIGN_EPOCHS` и
`range(10)` из-за отступов — alignment прошёл 15 эпох, η — 10 эпох полностью, это не
проблема, просто дольше). Пайплайн от свежего датасета **воспроизводится**:
- Alignment: val loss 1.33 → 0.21 за 15 эпох (сходится, M(h)≈h работает).
- η-голова: loss 0.176, eta[секрет]≈0.84 vs eta[мусор]≈0.11 (учится, как и раньше).
- Дистилляция (3 эпохи, 48 примеров): val_acc 0.20/0.15/0.20 — уровень шанса, ОЖИДАЕМО
  для 3 эпох на 48 примерах; полный прогон (40 эпох, 320 примеров) — в Ф2.
- Никаких NaN/крашей. `memory_distill.pt` сохраняется (будет перезаписан полным прогоном).

**Вывод Ф0:** окружение готово, датасет свежий, диагностика §1 получена (‖Δ_in‖≈1.21 —
докстринг завысил в 4-8 раз), протоколы сверены. Ф0 ЗАКРЫТА.

**Следующий шаг (гейт):** Ф1 — эксперимент 1a (прототип input-injection vs logit-shift).
Нужно решение пользователя на старт фазы + затем решение по результату 1a.
